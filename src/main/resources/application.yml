spring:
  datasource:
    url: jdbc:mysql://127.0.0.1/mocksns
    username: root
    password: l$de11@0$ql
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug